<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <title>{{ 'email.forgot_password.title'|trans }}</title>
    <style type="text/css">
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, sans-serif; }
        .btn-primary { 
            display: inline-block; 
            background-color: #FF6B35 !important; 
            color: #fff !important; 
            padding: 12px 24px; 
            text-decoration: none; 
            border-radius: 6px; 
            font-weight: 500; 
            transition: all 0.2s ease-in-out; 
        }
        .btn-primary:hover { 
            background-color: #F08A4B !important; 
            transform: translateY(-1px); 
            box-shadow: 0 2px 8px rgba(255, 179, 71, 0.3); 
        }
    </style>
</head>
<body>
    <p>{{ 'email.forgot_password.body'|raw|trans }}</p>
    <a href="{{ reset_url }}" class="btn-primary">{{ 'email.forgot_password.button_text'|trans }}</a>
    <p>{{ 'email.forgot_password.signature'|trans|nl2br }}</p>
</body>
</html>
